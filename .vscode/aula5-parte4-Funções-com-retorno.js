// São funções que retornam alguma coisa (algum valor, variável, entre outros)

let resultado = soma(5, 10) // --> atribui a uma variável o resultado de uma função

console.log('O resultado dessa função é ' + resultado)

function soma(numA, numB){
    return numA + numB
}